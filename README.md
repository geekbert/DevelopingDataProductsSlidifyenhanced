DevelopingDataProductsSlidifyenhanced
=====================================

In order to host content on git pages:

$ git branch gh-pages
$ git checkout gh-pages
$ git push origin gh-pages 

# View on: http://geekbert.github.io/DevelopingDataProductsSlidifyenhanced/#1
